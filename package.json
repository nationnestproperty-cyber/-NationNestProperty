{
  "name": "nationnest-mini",
  "private": true,
  "scripts": {
  "dev": "next dev",
  "build": "npm run vercel-build",
  "vercel-build": "prisma generate && echo GEN_OK && prisma db push && echo PUSH_OK && prisma db seed && echo SEED_OK && next build && echo BUILD_OK",
  "start": "next start"
},
  "dependencies": {
    "@prisma/client": "5.13.0",
    "bcryptjs": "2.4.3",
    "next": "14.2.5",
    "react": "18.2.0",
    "react-dom": "18.2.0"
  },
  "devDependencies": {
    "prisma": "5.13.0",
    "typescript": "5.4.5"
  },
  "prisma": { "seed": "node prisma/seed.cjs" }
}
